#include <stdio.h>

/**
 *main - EntryPoint
 *@argc: count
 *@argv: vector
 *
 *Return: always 0
 */

int main(int argc, char *argv[])
{
	int res;

	if (argc < 2)
	{
		printf("Error\n");
		return (1);
	}
	else
	{
		res = (int)(argv[1]) * (int)(argv[2]);
		printf("%d\n", res);
	}
	return (0);
}
